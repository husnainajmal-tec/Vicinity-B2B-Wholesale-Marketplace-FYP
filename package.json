{
  "name": "vicinity-trade",
  "version": "0.1.0",
  "private": true,
  "description": "Vicinity Trade — a B2B wholesale marketplace connecting Sellers (manufacturers/wholesalers) with Buyers (retailers/bulk buyers).",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently -n server,client -c blue,magenta \"npm run server\" \"npm run client\"",
    "seed:admin": "npm run seed:admin --prefix server",
    "seed:demo": "npm run seed:demo --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
